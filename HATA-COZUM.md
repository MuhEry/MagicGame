# HATA - COZUM

Ortak hata defteri. **Kural: her hata + cozumu TEK SATIR.** Uzun aciklama yazma.

> Bu dosya `Assets/_Project/HATA-COZUM.md` ile birlestirildi. Tek dosya kalsin diye
> oradaki iki kayit (#1 ve #2) buraya tasindi, o kopya silindi.

## Ortam (once bunu oku)

| Ne | Surum |
|---|---|
| Unity | `6000.3.21f1` |
| XR Interaction Toolkit | **`3.4.1` (XRI 3.x)** |
| Render Pipeline | URP `17.3.0` |
| XR Hands | `1.7.3` |
| OpenXR | `1.16.1` |

**XRI 3.x farklari — 2.x ornegi kopyalamadan once oku:**

- `CanSelect` imzasi `CanSelect(IXRSelectInteractable)`. 2.x'teki `CanSelect(XRBaseInteractable)` overload'i `XRBaseInteractor.deprecated.cs` icinde, kullanma.
- Namespace'ler bolundu: `...Toolkit.Interactors` (XRSocketInteractor, XRBaseInteractor) / `...Toolkit.Interactables` (XRGrabInteractable, IXRSelectInteractable) / `...Toolkit` (SelectEnterEventArgs, XRInteractionManager).
- Haptik: 2.x'teki `XRBaseController.SendHapticImpulse(a, d)` deprecated. 3.x'te `HapticsUtility.SendHapticImpulse(amplitude, duration, HapticsUtility.Controller.Both)` — namespace `UnityEngine.XR.Interaction.Toolkit.Inputs.Haptics`.
- `XRBaseControllerInteractor` deprecated -> `XRBaseInputInteractor`.

---

## Kayitlar

| # | Kim | Hata | Cozum |
|---|---|---|---|
| 1 | C | `XRBaseControllerInteractor` XRI 3.0+ ile deprecated uyarisi | `XRBaseInputInteractor` ile degistirildi. |
| 2 | C | `ItemCategory` / `DecisionResult` hem `Core/` hem `Shared/` altinda tanimliydi -> `CS0101` duplicate, proje derlenmiyordu | Dosyalar `Shared/` altinda birakildi, `Core/` kopyalari silindi. |
| 3 | B | Play acikken script derlendi -> XRI bilesenlerinde `NullReferenceException: routine is null` (XRSocketInteractor.OnEnable) ve `XRGrabInteractable.SubscribeTeleportationProvider` NRE | Play'i kapat, derleme bitsin, sonra Play'e bas. `Awake`'te atanan alanlar domain reload'da null kalir; kod hatasi degil. |
| 4 | B | Sokete kategori filtresi koyunca yanlis esya hic girmiyor, oyuncu geri bildirim alamiyor | `CanSelect`'i kategoriye gore KISITLAMA. Yanlis esyayi da kabul et, karari `OnSelectEntered`'da ver, yanlissa 0,4 sn sonra `SelectExit` + impulse. |
| 5 | B | Yanlis esya soketten cikinca havada asili kaliyor | `SelectExit`'ten sonra bir kare bekleyip `rb.isKinematic = false` yap, sonra `AddForce`. |
| 6 | B | URP'de emissive rengi runtime'da degismiyor | Materyalde Emission ACIK olmali (`EnableKeyword("_EMISSION")`, `globalIlluminationFlags = RealtimeEmissive`). Rengi `MaterialPropertyBlock` ile ez, materyali klonlama. |
| 7 | B | Dogru esya sokete girince yok olmuyor, arka arkaya "DOGRU" sinyali basiliyor | `AcceptRoutine` esyayi birakip bir kare bekliyordu; o boslukta soket ayni esyayi tekrar yakaliyor, yeniden giris `StopCoroutine` ile rutini `Destroy`'a varmadan olduruyordu. Esyayi ONCE `SetActive(false)` ile etkilesim disina al, SONRA birak; rutin calisirken yeniden baslatma. |
| 8 | B | Esya dolabin icindeyken tikirti sesi caliyor / sallama sayiliyor | `XRGrabInteractable.isSelected` soket tuttugunda da `true` doner. Tutan interactor'in `XRSocketInteractor` olup olmadigina bak; yoklama kanallari yalnizca ELDE calissin. |
| 9 | B | `SelectExit()` cagrisi `OnSelectExited`'i SENKRON tetikliyor; oradaki `StopCoroutine` cagiran coroutine'i kendi icinden olduruyor | Temizlik dallarini bir bayrakla (`m_Rejecting` / `m_Accepting`) koru, yoksa `SelectExit` sonrasi kod hic calismaz. |
| 10 | B | Spawn sirasi her turda ayni (seed sabit `12345`) | `seed = 0` -> `Environment.TickCount`'tan tohum. `UnityEngine.Random` KULLANMA (mimari kural 4); Faz 2'de host `SetSeed(...)` ile istemciye ayni degeri gecirir. Kullanilan seed Console'a loglaniyor. |
| 11 | Ekip | `Main.unity` bosti (2 obje), Build Settings `Assets/Scenes/SampleScene.unity`'yi isaret ediyordu -> APK'da bos ekran | `Tools > Kayip Esya > Main Sahnesini Kur` menusu sahneyi kurar ve Build Settings'i duzeltir. |

---

## Faz 2 (Alteruna) kayitlari

**Kurulu surum: Alteruna Multiplayer SDK `2.1.1003`.** Asagidaki API notlari bu surume gore
dogrulandi; SDK yukseltilirse once bu tablo tekrar kontrol edilmeli.

| # | Kim | Hata | Cozum |
|---|---|---|---|
| 46 | Faz2 | **DONMANIN GERCEK SEBEBI: `MultiplayerManager.Host()`.** Unity `Responding: False` durumuna dusuyor, log Host() cagrisinin hemen ardinda susuyor, crash dump olusmuyor. Sahnede Alteruna'nin hazir **`Room Menu.prefab`** ornegi vardi (paket guid `5a577542a281e0a448dc3b383023e1f2`); Start butonu `Host()` cagiriyor | `Host()` icinde `Service.ClosePort()` var ve ClosePort ana is parcaciginda **`Task.Wait()`** calistiriyor. Task tamamlanmazsa editor kilitlenir. SDK'nin tamami tarandi: **`Task.Wait()` yapan TEK yol `Host()`**tir; `Connect()`, `CreateRoom()`, `JoinLan()`, `DirectConnect()` temizdir. Cozum: `HostLanSession()` artik `Host()` YERINE `Connect()` + `CreateRoom()` kullaniyor (ikisi de ClosePort'a dokunmaz). Ayrica `Tools > Gece Vardiyasi > Alteruna Hazir Menusunu Kapat` komutu sahnedeki RoomMenu/RoomBrowser/MatchmakingMenu objelerini kapatir; kontrol listesi acik kalani hata olarak raporlar. **Kendi butonunuzu HostLanSession()'a baglayin, Alteruna'nin hazir menusunu KULLANMAYIN.** |
| 45 | Faz2 | **DUZELTME - kayit #30 eksikti.** `ConnectOnStart = false` yapmak ag trafigini DURDURMUYOR. Bu SDK surumunde `ConnectOnStart` yalnizca yapicida ve `GetDebuggingInfo` metninde geciyor; hicbir davranisi kosullandirmiyor (IL ile dogrulandi). Unity'nin otomatik cagirdigi `MultiplayerManager.Start()` kosulsuz olarak `Service.Start()` + `OpenPort()` calistiriyor - Play'e basar basmaz soketler aciliyor ve LAN kesfi basliyor. Oyun "biraz calisip sonra donuyordu" | **Bileseni KAPALI birak.** Unity devre disi bilesende `Start()` cagirmaz, hicbir soket acilmaz. `Awake` yine calisir (zararsiz), `FindObjectsOfType` devre disi bileseni bulmaya devam ettigi icin kopru `Multiplayer` referansini kaybetmez. Butona basildiginda SDK kendini ayaga kaldirir: hem `Host()` hem `Connect()` icinde `enabled = true` + `Start()` var. Komut: `Tools > Gece Vardiyasi > Agi Play'de Kapali Baslat`. Kontrol listesi bileseni denetliyor. **Arama yaparken `FindObjectsInactive.Include` sart** - yoksa kapali bilesen bulunamaz. |
| 44 | Ekip | **XR yapilandirmasi bozuktu: her iki hedefte de `m_Loaders` BOSTU.** Yani hicbir XR loader kayitli degildi - APK'da VR HIC olmazdi, editorde de XR ayaga kalkamazdi. Ustelik Standalone'da `m_InitManagerOnStart: 1` idi ve Play'e basinca editor sessizce kapaniyordu | Aktif dosya `Assets/XR/XRGeneralSettingsPerBuildTarget.asset`. `faz2-final` dalindaki calisan yapilandirmayla karsilastirilip birebir esitlendi: **Android Providers** -> `OpenXRLoader` + `AutomaticLoading/Running = 1`; **Standalone Providers** -> `OpenXRLoader` + `0/0`; **Standalone Settings** -> `InitManagerOnStart = 0` (editorde XR yok, Play'de olmuyor); **Android Settings** -> `1` (APK'da VR var). Loader guid `17afc41331b61634396db4295dfabe3b` = `Assets/XR/Loaders/OpenXRLoader.asset`. **Not:** projede Oculus XR paketi YOK, yalnizca OpenXR var; eski notlardaki "Oculus Loader" onerisi bu dal icin gecersizdir. Unity KAPALIYKEN duzenlendi. |
| 43 | Ekip | Console'da surekli `Hand Tracking Subsystem not found or not running` uyarisi; el takibi ile kontrolcu arasinda hangisinin kullanildigi belirsizdi | **Bu oyun KONTROLCU ile oynanir, el takibi KAPALI olmali.** Sebep sartnamede: uc yoklama kanalindan biri haptiktir ("Agirlik: tutulurken surekli haptik"). El takibinde haptik YOKTUR - oyuncu el moduna gecerse oyunun ucte birini kaybeder. `Tools > Gece Vardiyasi > El Takibini Kapat (yalnizca kontrolcu)` komutu Standalone+Android'de OpenXR `HandTracking` ozelligini kapatir ve `OculusTouchControllerProfile`i acik tutar. **Kontrolcu profili kapatilmamali**: kapaliysa OpenXR girdileri hicbir action'a baglamaz ve gozlukte hicbir sey algilanmaz (bu profil su an zaten acikti). |
| 42 | Ekip | Play'e basinca Unity "patladi": arka arkaya `NullReferenceException: routine is null` (XRSocketInteractor.OnDisable) ve `KeyNotFoundException` (TrackedDeviceGraphicRaycaster.OnDisable). Oyun kodunda hicbir sey degismemisti | **Kok sebep derleme hatasiydi.** `Assets/_Project/Editor/Faz2Setup.cs` icinde `CS0246: XRBaseInputInteractor` vardi (`Interactors` yerine `Interactables` using'i). `Assembly-CSharp-Editor.dll` HIC derlenmedi (`Tundra build failed, ExitCode 1`), Unity son basarili domain ile calisti ve Play'e basilinca XRI bilesenleri tutarsiz duruma dustu; istisnalarin HEPSI `OnDisable` icinde, yani cikista patladi. **Kural: Console'da derleme hatasi varken Play'e BASMA.** Hatayi duzelt, derleme bitsin, sonra Play. Istisnalar XRI'in kendi icinden gelir - bizim mantik hatamiz degildir (bkz. kayit #3). |
| 41 | Faz2 | Play'e basinca Console'da KIRMIZI `Project query failed: Unregistered. Please register your project under Window->Alteruna->Register Project.` cikiyor. Kayit yapmadan oynanamaz saniliyor | **Yanlis alarm - LAN oyununu ENGELLEMEZ.** `MultiplayerManager.Awake` icinde `VerifyProjectID` var; bu buluta ASENKRON bir HTTP sorgusudur ve `ConnectOnStart` kapali olsa da calisir. LAN zinciri bu sorguya hic bakmaz: `Host()` -> `OpenPort()` + `JoinOwnRoom()`, `JoinLan()` -> `Connect()`, `DirectConnect()` - hicbiri API'yi cagirmaz (IL ile dogrulandi). Kritik nokta `Service.A()`: `if (!IsAuthenticated) return 3;` yani **kayitsiz projede oda siniri 3 kullanicidir**, oda kurulurken `MaxUsers = Min(2, 3) = 2` olur. Kayit yalnizca BULUT ozelliklerini (oda listesi, matchmaking, uzak sunucu) acar - biz kullanmiyoruz. Hatayi tamamen susturmak isterseniz portal.mp2.alteruna.com'dan kayit olun; ama **Application ID `Assets/Alteruna/Resources` altina yazilir ve o klasor Alteruna'nin kendi .gitignore'unda** - yani herkes kendi makinesinde ayri ayri kaydolmak zorunda kalir. |
| 38 | Faz2 | "Transport = UDP yapin" diyen kaynaklar var ama ayarda UDP secenegi YOK; ekip yanlis yerde ayar aradi | Alteruna 2.1'de `TransportType` = NaN / Default / **TCP** / TCPS / WebSocket. UDP yoktur. Dogru deger TCP (WebSocket yalnizca WebGL). **LAN kesfi AYRI bir ayardir** (`LAN Discovery`) ve UDP yayinini kendi icinde kullanir. Kontrol komutu artik `Assets/Resources/AlterunaConfig.asset` dosyasini okuyup bunlari denetliyor. |
| 39 | Faz2 | `Assets/Resources/AlterunaConfig.asset` git'e HIC eklenmemisti (ignore edilmiyor, sadece commit edilmemis). LAN Discovery / transport / port ayarlari bu dosyada; ekipteki digerlerinde hicbiri yoktu | Dosyayi commit edin. Kontrol komutu her calistiginda bunu hatirlatiyor. |
| 40 | Faz2 | Player Settings > Other Settings > **Internet Access = Auto** idi (`ForceInternetPermission: 0`). Auto'da Unity ag iznine ihtiyac olup olmadigini tahmin eder; Alteruna bu tahmine takilir - editorde calisir, gozlukte hic baglanmaz | `Tools > Gece Vardiyasi > Android Ag Iznini Zorunlu Yap` komutu `Require` yapar. ProjectSettings degisikligi oldugu icin kurulum komutuna dahil DEGIL, ayri ve istenerek calisir. |
| 37 | Faz2 | Kurulum komutu `PlayerRefs`e sol/sag eli baglayamiyordu ("EKSIK: sol/sag el bagli"), avatarin elleri hareketsiz kalirdi | El secimi INTERACTOR'IN ADINDA GECMEZ. XRI 3.x Hands rig'inde interactor'larin adi "Poke Interactor" / "Near-Far Interactor"dir; sol/sag ayrimi bir ust seviyedeki **"Left Hand" / "Right Hand"** objesindedir. Cozum `PlayerRefs.FindHandTransforms`: tum transformlari tarar ve ad benzerligine gore PUANLAR ("left hand" > "left controller" > "left"). Kontrol komutu artik bulunan obje adlarini da yazar. |
| 30 | Faz2 | **Play'e basinca Alteruna aninda kilitliyordu.** `MultiplayerManager.ConnectOnStart` ACIKTI: Play aninda BULUTA baglanmayi deniyor. Projede kayitli Application ID / lisans yok (`Assets/Alteruna/Resources` bos ve zaten `.gitignore`da), dolayisiyla deneme cevapsiz kaliyor; XR baslatmasiyla ust uste binince editor kilitlenmis gorunuyordu | `ConnectOnStart = false`. Iki gozluk ayni odada oldugu icin buluta HIC ihtiyac yok: baglanti yalnizca oyuncu Host / JoinLan'a bastiginda LAN uzerinden kurulur. Kurulum komutu bunu otomatik yapar, kontrol komutu denetler. **Bulut yolu = lisans + internet + oda listesi + sunucu gecikmesi; LAN'dan buluta gecmek kolay, tersi degil.** |
| 31 | Faz2 | `JoinFirstAvailable()` ile odaya katilmaya calisiliyordu; bu cagri BULUT oda listesini kullanir, lisans/internet ister ve LAN testinde "connecting"te takilir | LAN kesfi icin dogru cagri `Multiplayer.JoinLan()`. Akis: Gozluk A `Host()`, Gozluk B `JoinLan()`. Kesif calismazsa `DirectConnect(<host-ip>)`. |
| 32 | Faz2 | **Editorde calisiyor, gozlukte hicbir sey senkron olmuyor** - hicbir hata, hicbir log yok | `[SynchronizableMethod]` RPC'leri YANSIMA ile bulunur. IL2CPP + ARM64 kirpicisi bu metotlara cagri goremeyip onlari SESSIZCE siler. `Assets/link.xml` eklendi (Alteruna + Assembly-CSharp `preserve="all"`). Alternatif: Player Settings > Managed Stripping Level = Low. |
| 33 | Faz2 | Kurum/okul Wi-Fi'sinde iki gozluk birbirini hic gormuyor | **AP izolasyonu** (client isolation) kurumsal ve misafir aglarda standart olarak ACIKTIR; ayni agdaki istemcilerin birbirine paket gondermesini, dolayisiyla UDP yayinini engeller. Ag yoneticisinden kapattirmak genelde mumkun degil. **Telefon hotspot'u acin, iki gozlugu ona baglayin** - hotspot'ta izolasyon yoktur. Bunu yedek plan degil, BIRINCIL plan yapin. |
| 34 | Faz2 | Esya iki el arasinda titriyor / "lastik gibi" ileri geri zipliyor | Iki istemci ayni transformu ayni anda yaziyor. Kavrama aninda `TakeOwnership()`, birakmada `ReleaseOwnership()` cagrilir (`ItemOwnership`). Ayrica kilit sahibi olmayan cihazda `XRGrabInteractable` kapatilir. |
| 35 | Faz2 | Uzak avatarin kafasi/elleri kayik duruyor | `TransformSynchronizable` varsayilan olarak YEREL konumu senkronlar; iki cihazda avatar kokleri birebir ayni yerde degilse kayar. Avatar parcalarinda `UseGlobalPosition = true`. Eller kafadan hizli hareket ettigi icin `RefreshRate` elde 40, kafada 20. |
| 36 | Faz2 | Sahne kaydedilmeden alinan derleme, karsi tarafin nesnelerini tanimiyor | Alteruna bilesenleri kendi kimliklerini (UID) SAHNE DOSYASINA yazar. **Derleme oncesi Ctrl+S pazarlik konusu degil.** Ayrica iki gozluge AYNI APK kurulmali; farkli derlemeler farkli nesne kimlikleri uretir ("bende calisiyordu" cumlesinin en sik sebebi budur). |
| 12 | Faz2 | **Play'e basinca editor doniyor/kapaniyordu.** `Editor-prev.log`'da hicbir istisna yok; log her seferinde ayni yerde susuyor: `[Subsystems] Loading plugin UnityOpenXR for subsystem OpenXR Input...`. Sucu Alteruna sanildi, degildi | Cokme degil: gozluk Link ile bagliyken Unity'nin ana dongusu gozlugun kare temposuna kilitlenir. `Tools > Gece Vardiyasi > Editorde XR Baslatmayi Kapat (Standalone)` komutu YALNIZCA Standalone hedefinde "Initialize XR on Startup"i kapatir. **Android ayari degismez, APK'da VR aynen calisir.** Editorde Link ile test etme, `Build & Run` kullan. |
| 13 | Faz2 | Faz 2 kurulumu `[InitializeOnLoadMethod]` ile Main.unity her acildiginda sahneyi degistirip **kaydediyordu**; herkeste farkli bir Main.unity olustu, elle yapilan duzeltmeler bir sonraki acilista geri alindi | Otomatik calisan kod YOK. Kurulum yalnizca `Tools > Gece Vardiyasi > Faz 2 Kurulumunu Uygula` ile, istenerek calisir; sahneyi **kaydetmez**, yalnizca kirli isaretler. |
| 14 | Faz2 | `Multiplayer.Host()` gibi cagrilar SDK surumleri arasinda degisiyor; eski surume gore yazilan kod yeni pakette derlenmiyor | Oda API'si su an: `Host()` (servis + oda), `CreateRoom()` (servis zaten ayaktaysa), `JoinFirstAvailable()`, `DirectConnect(ip)`. **Ezberden yazma** - `Library/PackageCache/com.alteruna.multiplayer@*/Scripts/RoomMenu/RoomMenu.cs` gercek kullanimi gosterir. |
| 15 | Faz2 | `[SynchronizableMethod]`in hangi `using`'e ait oldugu bulunamiyordu; hicbir namespace'te gorunmuyor | Bu bir NAMESPACE tipi degil, `AttributesSync` (ve `Synchronizable`) **icinde ic ice tanimli** bir tip. Bu siniflardan turetince `using` olmadan cozulur. |
| 16 | Faz2 | `AttributesSync`ten turetirken `OnEnable` gizlendi (CS0114). `CommunicationBridgeUID.OnEnable` synchronizable'i KAYDEDEN metottur; gizlenince bilesenin butun RPC'leri sessizce hic calismaz | `public override void OnEnable()` + `base.OnEnable()`. `OnDestroy` de ayni sekilde override. Base'de `OnDisable` YOK, o override degil. **Kural: Alteruna sinifindan turerken Unity mesajlarini override et, gizleme.** `AttributesSync` ayrica `LateUpdate` tanimlar - onu da gizleme. |
| 17 | Faz2 | `Avatar` adi hem Alteruna'da hem `UnityEngine`de (animasyon) var; `using UnityEngine;` ile birlikte CS0104 belirsizlik hatasi | Takma ad kullan: `using AlterunaAvatar = Alteruna.Multiplayer.Unity.Avatar;`. Ayrica `XRGeneralSettings` -> `UnityEngine.XR.Management`, `XRGeneralSettingsPerBuildTarget` -> `UnityEditor.XR.Management` (farkli namespace). |
| 18 | Faz2 | `MultiplayerManager.AvatarPrefab` sahnedeki AKTIF XR rig'ini gosterirse Alteruna onu klonlar; ayni UID iki nesnede olur (`Synchronizable already registered ... (Clone)`), orijinal rig senkron gonderemez ve sahnede iki AudioListener olusur. Rig'i pasiflestirmek de baska bir sorun uretir: odaya girilmezse sahnede hic kamera kalmaz, gozlukte siyah ekran | **XR rig hic klonlanmaz.** Avatar AYRI ve hafif bir prefabdir (kafa + 2 el, `NetworkPlayerAvatar.prefab`). Yerel rig HER ZAMAN aktif kalir, aga hic sokulmaz - Faz 1 akisi bozulmaz. Yerel avatar kopyasi `NetworkAvatarRig` ile rig'i takip eder, uzak kopya `TransformSynchronizable` ile poz alir. |
| 19 | Faz2 | Alteruna'nin uzak avatar temizligi her alt `Behaviour` icin `type.Namespace.Length` okur. Bu projedeki scriptlerin hepsi global namespace'te oldugu icin `Namespace` NULL doner -> `NullReferenceException`, temizlik yarida kalir | XR rig'in ALTINA kendi scriptlerimizi ekleme. `PlayerRefs` rig yerine `Systems` altinda durur ve rig transformlarina yalnizca REFERANS tutar. `Faz 2 Kurulumunu Kontrol Et` rig altindaki namespace'siz scriptleri hata olarak raporlar. |
| 20 | Faz2 | Iki oyuncu ayni anda ayni esyaya uzaninca ikisinin de cihazinda kavrama basarili oluyor; iki senkron bileseni ters pozisyon gonderip esyayi titretiyor | `ItemOwnership` artik `AttributesSync`: esya ELE alininca agda sahiplik ilan edilir, digerinin cihazinda `XRGrabInteractable` kapatilir; cekisme hic baslamaz. Soket (dolap) tutuslari kilidin disindadir. |
| 21 | Faz2 | `NetworkShiftCoordinator.Subscribe()` yalnizca `Start()`ta cagrilirsa `Multiplayer` o an null oldugunda bir daha HIC denenmez: `OnRoomJoined` gelmez, `IsInRoom` sonsuza kadar false kalir ve iki gozluk de sessizce CEVRIMDISI oynar | `Update()` icinde `if (!subscribed) TrySubscribe();`. Kopru kurulunca Console'a `[Network] Alteruna kopru baglandi` yazilir. |
| 22 | Faz2 | `ItemSpawner`, Alteruna `Spawner.Spawn(index, ...)` cagrisina kendi listesinin indeksini gonderiyordu. Alteruna indeksi KENDI `SpawnableObjects` listesine gore cozer; iki liste ayni sirada olmazsa host bir esyayi, istemci bambaska bir esyayi gorur | Indeks `networkSpawner.SpawnableObjects.IndexOf(prefab)` ile cozulur; bulunamazsa esya uretilmez ve `LogError` basilir. Kurulum komutu iki listeyi ayni sirayla doldurur. |
| 23 | Faz2 | Odadayken `networkSpawner` atanmamissa kod sessizce yerel `Instantiate`e dusuyordu: host esyayi goruyor, istemci bos tezgaha bakiyordu | Odadayken spawner yoksa `LogError` + esya uretilmez. **Sessiz desenkronizasyon yerine gorunur hata.** |
| 24 | Faz2 | Dogru esyanin tuketilmesi her cihazda kendi yerel soket olayindan tetikleniyordu. Iki gozlukte fizik birebir ayni olmadigi icin esya birinde kayboluyor, digerinde tezgahta kaliyordu | Host karari uyguladiktan sonra `BroadcastItemConsumed(itemId)` yayinlar. `CategorySocket` bu olayi dinler ve esya hala duruyorsa kaldirir; zaten kaldirilmissa hicbir sey yapmaz (idempotent). |
| 25 | Faz2 | Skoru istemci de kendi hesaplarsa iki gozlukte iki farkli skor olusuyor | Skor/sure/kalan esya yalnizca HOST'ta islenir (mimari kural 1). Istemci `RegisterDecision`i host'a ISTEK olarak gonderir; host kendi sahnesindeki nesneden dogru kategoriyi DOGRULAR ve sonucu `ApplyDecision` ile herkese ayni sirayla yayinlar. Istemcinin bildirdigi "dogru kategori"ye guvenilmez. |
| 26 | Faz2 | Her derlemeden sonra **"Missing Dependency — Alteruna Multiplayer SDK is required but was not detected"** penceresi cikiyordu; SDK asli kurulu | Kontrolcu `Assets/Samples/XR Interaction Toolkit/3.4.1/Starter Assets/Editor/Plugins/AlterunaDependencyCheck.dll` icinde ve SDK **v1**'in tip adini ariyor (`Alteruna.Multiplayer.MultiplayerManager, Alteruna`). Kurulu 2.1'de tip `Alteruna.Multiplayer.Unity.MultiplayerManager`, yani kontrol HER ZAMAN basarisiz. Yanlis alarm; DLL silinebilir. **`Download`a BASMAYIN:** SDK'nin ikinci kopyasini `Assets/` altina indirir, her tip iki kez tanimlanir (`CS0101`) ve proje hic derlenmez. |
| 28 | Git | `.gitattributes` `*.unity`/`*.prefab`/`*.asset`/`*.meta` icin `merge=unityyamlmerge` diyor ama surucu git config'de **tanimli degildi**. Git tanimadigi surucuyu sessizce yok sayip SATIR BAZLI metin birlestirmesi yapar: sahne "birlesmis" gorunur, aslinda bozuktur | Her klonda BIR KEZ: `git config merge.unityyamlmerge.driver "'<Unity>/Editor/Data/Tools/UnityYAMLMerge.exe' merge -p --force --fallback-none %O %B %A %A"` + `.name` ve `.recursive binary`. Bu ayar `.git/config`tedir, **push ile gitmez** - herkes kendi bilgisayarinda yapmali. |
| 29 | Git | Faz 2 kurulum komutu Main.unity'yi, 9 esya prefabini ve yeni avatar prefabini degistirir. Iki kisi ayri ayri calistirirsa avatar prefabi iki farkli GUID ile olusur ve sahne/prefab catismasi cikar | Kurulumu **yalnizca C (repo sorumlusu) bir kez** calistirir ve commit eder; digerleri sadece `pull` yapar. Sartnamedeki "Main.unity'ye YALNIZCA C dokunur" kurali Faz 2 icin de gecerli. |
| 27 | Arac | Unity ACIKKEN `dotnet build Assembly-CSharp.csproj` calistirmak ciktiyi Unity'nin kendi `Temp/bin/Debug/` klasorune yazar, derleme hattiyla cakisir ve editoru "Compiling Scripts"te dondurabilir | Unity acikken `dotnet build` calistirma. Hizli sozdizimi kontrolu gerekiyorsa `csc`yi `-out:` ile proje DISINA yonlendir (`Temp/` ve `obj/`ye dokunma). Donarsa Unity'yi kapat, `Temp/` ve `obj/` sil, yeniden ac. |
